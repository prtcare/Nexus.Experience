<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Nexus.Products.Chat.Domain</RootNamespace>
    <AssemblyName>Nexus.Products.Chat.Domain</AssemblyName>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- DELIBERATE BREAK (M-08-1.4): pulls the Dataverse storage client into the
       domain, exactly the "domain must not know how it is stored" violation
       BoundaryTests.Domain_MustNotReference_Dataverse forbids. Reverted next
       commit. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.PowerPlatform.Dataverse.Client" Version="1.2.26" />
  </ItemGroup>

</Project>

